import React, { Component } from 'react';
import Page from './Page';

export default ({onClick}) => {
    return (
      <Page>
        <div className="InviteContainer">
          <p>こんにちは。画面をタッチしてください</p>
        </div>
      </Page>
    );
}
